services:
  - type: web
    name: skincare-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir back-end app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
